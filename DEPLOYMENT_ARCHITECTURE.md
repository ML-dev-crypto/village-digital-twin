# 🎨 RuralLens Deployment Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER'S BROWSER                            │
│                                                                  │
│  ┌──────────────────────┐        ┌──────────────────────┐      │
│  │  RuralLens Frontend  │        │  Sensor Simulator    │      │
│  │   (React + Vite)     │        │    (HTML/JS)         │      │
│  │                      │        │                      │      │
│  └──────────┬───────────┘        └──────────┬───────────┘      │
│             │                               │                   │
│             │ HTTPS                         │ WebSocket         │
│             ▼                               ▼                   │
└─────────────┼───────────────────────────────┼───────────────────┘
              │                               │
              │                               │
┌─────────────▼───────────────────────────────▼───────────────────┐
│                    RENDER.COM HOSTING                            │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  FRONTEND - Static Site (Free Tier)                      │  │
│  │  ────────────────────────────────────────────────────     │  │
│  │  • URL: https://ruralens-frontend.onrender.com           │  │
│  │  • Build: npm install && npm run build                   │  │
│  │  • Serves: React SPA from /dist folder                   │  │
│  │  • Auto-deploy: ✅ On git push                           │  │
│  │  • SSL: ✅ Free HTTPS                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│                             │                                    │
│                             │ WebSocket (WSS)                   │
│                             ▼                                    │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  BACKEND - Web Service (Free Tier)                       │  │
│  │  ────────────────────────────────────────────────────     │  │
│  │  • URL: https://ruralens-backend.onrender.com            │  │
│  │  • Port: 10000                                            │  │
│  │  • Runtime: Node.js + Express + WebSocket                │  │
│  │  • Auto-deploy: ✅ On git push                           │  │
│  │  • Sleep: ⚠️  After 15 min (Free tier)                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                              │
                              │ Data Generation
                              ▼
                    ┌─────────────────────┐
                    │  Simulated Sensors  │
                    │  ─────────────────  │
                    │  • Water Tanks (5)  │
                    │  • Power Nodes (12) │
                    │  • IoT Sensors (18) │
                    │  • Traffic Signals  │
                    │  • Environment      │
                    │  • Flood Monitors   │
                    └─────────────────────┘
```

---

## Data Flow Diagram

```
┌────────────────┐
│ Sensor         │
│ Simulator      │◄──── User adjusts slider
│ (HTML)         │
└────────┬───────┘
         │
         │ WebSocket Send
         │ { type: 'manual_update', ... }
         ▼
┌────────────────────┐
│ Backend Server     │
│ (Node.js + WS)     │◄──── Also generates data every 5s
│                    │
│ • Validates data   │
│ • Updates state    │
│ • Broadcasts       │
└────────┬───────────┘
         │
         │ WebSocket Broadcast
         │ { type: 'sensor_update', data: {...} }
         ▼
┌────────────────────┐
│ Frontend App       │
│ (React)            │
│                    │
│ • Updates UI       │
│ • Shows in charts  │
│ • Updates 3D map   │
└────────────────────┘
```

---

## Deployment Flow

```
┌──────────────┐
│  Developer   │
│  (You!)      │
└──────┬───────┘
       │
       │ git add .
       │ git commit -m "Changes"
       │ git push origin main
       ▼
┌──────────────────────┐
│  GitHub Repository   │
│  Abhishekmishra2808/ │
│  village-digital-    │
│  twin                │
└──────┬───────────────┘
       │
       │ Webhook triggers
       ▼
┌──────────────────────────────────────┐
│  Render.com Auto-Deploy              │
│                                      │
│  ┌────────────┐    ┌──────────────┐ │
│  │  Backend   │    │  Frontend    │ │
│  │  Rebuild   │    │  Rebuild     │ │
│  │  ~2-5 min  │    │  ~3-7 min    │ │
│  └─────┬──────┘    └──────┬───────┘ │
│        │                  │          │
│        │ Deploy           │ Deploy   │
│        ▼                  ▼          │
│  ┌─────────┐        ┌──────────┐    │
│  │ Live ✅ │        │ Live ✅  │    │
│  └─────────┘        └──────────┘    │
└──────────────────────────────────────┘
       │
       │ Auto-updated!
       ▼
┌──────────────┐
│  Users see   │
│  latest      │
│  version     │
└──────────────┘
```

---

## Environment Configuration

```
┌─────────────────────────────────────────────────────────┐
│                  DEVELOPMENT                             │
│  ─────────────────────────────────────────────────────   │
│                                                          │
│  Frontend:  http://localhost:5173                       │
│  Backend:   ws://localhost:3001                         │
│  Simulator: ws://localhost:3001                         │
│                                                          │
│  File: .env.development                                 │
│    VITE_WS_URL=ws://localhost:3001                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                  PRODUCTION                              │
│  ─────────────────────────────────────────────────────   │
│                                                          │
│  Frontend:  https://ruralens-frontend.onrender.com      │
│  Backend:   wss://ruralens-backend.onrender.com         │
│  Simulator: wss://ruralens-backend.onrender.com         │
│                                                          │
│  File: .env.production                                  │
│    VITE_WS_URL=wss://ruralens-backend.onrender.com      │
└─────────────────────────────────────────────────────────┘
```

---

## Project File Structure

```
village-digital-twin/
│
├── backend/                     ← Deploy as WEB SERVICE
│   ├── server.js               (Entry point)
│   ├── package.json            (Dependencies)
│   ├── render.yaml             (Render config)
│   └── utils/
│       └── dataGenerator.js    (Sensor data)
│
├── src/                         ← Build to /dist
│   ├── App.tsx                 (Main app)
│   ├── main.tsx                (Entry point)
│   ├── components/             (React components)
│   ├── hooks/
│   │   └── useWebSocket.ts     (WS connection)
│   └── store/
│       └── villageStore.ts     (State management)
│
├── dist/                        ← Deploy as STATIC SITE
│   ├── index.html              (Generated by build)
│   └── assets/                 (JS, CSS, images)
│
├── sensor-simulator.html        ← Can host on static site
│
├── package.json                (Frontend dependencies)
├── vite.config.ts              (Build config)
├── .env.development            (Local dev config)
└── .env.production             (Production config)

DEPLOYMENT GUIDES:
├── RENDER_DEPLOYMENT.md        ← Full guide
├── DEPLOYMENT_CHECKLIST.md     ← Quick checklist
└── DEPLOY_QUICK_REF.md         ← Quick reference
```

---

## Service Communication

```
┌─────────────────────────────────────────────────────┐
│  Frontend (React App)                               │
│  ─────────────────────────────────────────────────  │
│                                                     │
│  useWebSocket.ts connects to:                      │
│  • Development: ws://localhost:3001                │
│  • Production:  wss://ruralens-backend...com       │
│                                                     │
│  Auto-detects environment via:                     │
│    import.meta.env.PROD                            │
│    import.meta.env.VITE_WS_URL                     │
└────────────────────┬────────────────────────────────┘
                     │
                     │ WebSocket Connection
                     │ (Binary protocol, full-duplex)
                     ▼
┌─────────────────────────────────────────────────────┐
│  Backend (Node.js + Express + WS)                   │
│  ─────────────────────────────────────────────────  │
│                                                     │
│  server.js listens on:                             │
│  • HTTP: Port 10000 (Render assigns)               │
│  • WebSocket: Same port (upgraded connection)      │
│                                                     │
│  Broadcasts every 5 seconds:                       │
│  • Updated sensor data                             │
│  • Alert notifications                             │
│  • System statistics                               │
└─────────────────────────────────────────────────────┘
```

---

## Free Tier Resource Allocation

```
Backend Web Service (Free):
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  RAM:          512 MB
  CPU:          0.1 vCPU
  Build Time:   500 min/month (shared)
  Bandwidth:    100 GB/month
  Instances:    1 (auto-scales to 0 when idle)
  Sleep:        After 15 min inactivity ⚠️
  Cold Start:   30-60 seconds
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Frontend Static Site (Free):
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Storage:      Unlimited
  Bandwidth:    100 GB/month
  Build Time:   500 min/month (shared)
  CDN:          Global ✅
  SSL:          Free HTTPS ✅
  Downtime:     None (always on) ✅
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Monitoring & Logs

```
Render Dashboard → Your Service → Logs
  │
  ├─ Build Logs
  │  └─ npm install output
  │     └─ Build errors/warnings
  │
  ├─ Deploy Logs  
  │  └─ Deployment status
  │     └─ Container startup
  │
  └─ Runtime Logs (Live Tail)
     └─ console.log outputs
        └─ Error stack traces
           └─ WebSocket connections
              └─ Request/response logs

Access: https://dashboard.render.com/web/[service-id]/logs
```

---

## Success Indicators

```
✅ Backend Deployed Successfully:
   • Status: "Live" (green dot)
   • Logs show: "WebSocket server running on port 10000"
   • URL accessible: Shows "RuralLens Backend"
   • No errors in logs

✅ Frontend Deployed Successfully:
   • Status: "Live" (green dot)
   • URL shows: Full dashboard UI
   • Status bar: "Connected to Production Server"
   • Charts loading with data
   • 3D map rendering
   • No console errors

✅ End-to-End Working:
   • Sensor simulator can connect
   • Slider changes reflect in main app
   • Real-time updates every 5 seconds
   • All views accessible (Water, Power, etc.)
   • No WebSocket disconnections
```

---

**Visual Guide Created**: November 1, 2025
**Architecture Version**: 1.0.0
**Deployment Platform**: Render.com
**Status**: Production Ready 🚀
